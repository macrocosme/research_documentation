CREATE TABLE Scenario(
   ID INTEGER,
   OPENCL_PLATFORM INTEGER DEFAULT 0,
   OPENCL_DEVICE INTEGER DEFAULT 0,
   DEVICE_NAME VARCHAR(50) DEFAULT "arts1_25000",
   DEVICE_PADDING INTEGER DEFAULT 128,
   DEVICE_THREADS INTEGER DEFAULT 32,
   ITERATIONS INTEGER DEFAULT 7,
   MIN_THREADS INTEGER DEFAULT  8,
   MAX_THREADS INTEGER DEFAULT  1024,
   MAX_ITEMS INTEGER DEFAULT 63,
   MAX_UNROLL INTEGER DEFAULT 1,
   MAX_DIM0 INTEGER DEFAULT 1024,
   MAX_DIM1 INTEGER DEFAULT 128,
   MAX_ITEMS_DIM0 INTEGER DEFAULT 64,
   MAX_ITEMS_DIM1 INTEGER DEFAULT 32,
   SUBBANDING BOOLEAN DEFAULT 1,
   SNR VARCHAR(25) DEFAULT "MOMSIGMACUT",
   CHANNELS INTEGER DEFAULT 1536,
   MIN_FREQ DOUBLE(8,4) DEFAULT 1250.09765625,
   CHANNEL_BANDWIDTH INTEGER DEFAULT 0.1953125,
   SAMPLES INTEGER DEFAULT 12500,
   SAMPLING_TIME DOUBLE(8,4) DEFAULT 0.00008192,
   DOWNSAMPLING INTEGER DEFAULT 1,
   SUBBANDS INTEGER DEFAULT 32,
   SUBBANDING_DMS INTEGER DEFAULT 128,
   SUBBANDING_DM_FIRST DOUBLE(8,4) DEFAULT 5.0,
   SUBBANDING_DM_STEP DOUBLE(8,4) DEFAULT 6.4,
   DMS INTEGER DEFAULT 32,
   DM_FIRST DOUBLE(8,4) DEFAULT 0.0,
   DM_STEP DOUBLE(8,4) DEFAULT 0.2,
   BEAMS INTEGER DEFAULT 1,
   SYNTHESIZED_BEAMS INTEGER DEFAULT 1,
   INTEGRATION_STEPS INTEGER DEFAULT 5,
   ZAPPED_CHANNELS INTEGER DEFAULT NULL,
   MEDIAN_STEP INTEGER DEFAULT 5,
   NSIGMA INTEGER DEFAULT 3,
   PRIMARY KEY (ID)
);
