# SELECT - WHERE clause
# Selecting all employees of female gender. Returning all columns from the employees table (*).
# '=' is a comparison operator that operates on numbers and strings. When comparing strings, the comparison operator compares strings as a whole.

SELECT * 
FROM employees 
WHERE gender='F'; 