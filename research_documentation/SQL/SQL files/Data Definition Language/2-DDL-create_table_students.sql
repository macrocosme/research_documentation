# DDL - Create a new table called 'Students' with fields ID (set as primary key), FirstName, and LastName. 

CREATE TABLE Students(
 ID INTEGER,
 FirstName VARCHAR(50),
 LastName VARCHAR(50),
 PRIMARY KEY (ID)
);